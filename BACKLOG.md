# MadGigz backlog

Open items only. Anything not listed here has shipped — see `git log`.

Numbering is historical and deliberately not re-packed, so a number always means
the same thing across old conversations. Gaps are shipped items.

## 🚀 Public launch — critical set (target: end of Aug 2026)

Vir's call (13 Aug 2026): these are the items that **must** land before the
public launch at end of month. Everything else in the list is "great to have,"
not a blocker.

**Still open (9):** #137 (performance & scale), #95 (real-payments go-live),
#119 (terms & legal — lawyer pass), #134 (Universal Links), #118 (seed real
Madrid shows), #115 (flesh out fan profile), #116 (past-events poster wall —
code shipped, visual eyeball still pending), #129 (offline tickets), #110 (app
stores), #97 (tax & invoicing).

**Shipped from the critical set (19 Aug 2026):** #122 (branded OAuth ✅),
#111 (mass gig upload ✅), #140 (TUS resumable upload ✅), #58 (admin activity
tracking ✅).

Not in the launch set (nice-to-have after): #88, #90b, #92, #98, #99, #117,
#121, #124, #112, #125 (recurring ops).

## In suggested order

| Order | # | Item | What the work actually is | Blocked on | Size |
|---|---|---|---|---|---|
| ✅ | 153 | **Match a signed-up artist to a gig already on the site** | **Shipped 19 Aug 2026.** A gig added before its artist signed up has `artist_id` null and no `event_artists` link, so the artist can't post to it. New admin **Matches** page (`/admin/matches`) suggests links: an **approved** artist whose handle (artist_name or username, accent-insensitive) matches an untagged gig's **headliner** (`events.artist_name`) or a name in its **lineup**. Admin approves each with a **Tag artist** button → `tagArtistToEvent` writes the `event_artists` row (re-checks approved server-side, upsert so it's idempotent), which is exactly what puts the show on their profile and lets them post content. **Chose suggest-and-approve over auto-tag** — auto-tagging by name could mis-attribute a gig to a same-named artist, and tagging grants posting rights. Pure on-the-fly matching, no new table/migration (`fetchTagSuggestions`). **Follow-ups:** persist dismissals so a rejected non-match stops re-suggesting (needs a small table); optional auto-tag for exact high-confidence matches; fire a suggestion/notification at artist-approval time rather than only the standing page. | Done. | M |
| — | 152 | **Gig setlist — the songs an artist plays that night** | **Raised by Vir 19 Aug 2026.** Let an artist attach a **setlist** to a show — the songs they'll play (or played) that night — so a fan can see what to expect / remember the night. Shape TBD (Vir: "will work out how later"): likely an ordered list of song titles on the event, editable by the show's artist, shown on the event page (and maybe the fan's attended/poster-wall memory of it, #116). Open questions to settle when picked up: planned-vs-actual setlist, free-text vs linked tracks (Spotify?), who can edit (owner artist / tagged support acts), and whether it surfaces before or only after the show. No build yet — placeholder so it isn't lost. | Product design — Vir to define the shape. | M |
| 6 | 142 | **Feed algorithm — real "for-you" ranking** | **Raised by Vir 13 Aug 2026.** The pitch is literally *"a for-you page for local gigs"*, so ranking is core identity, not a nice-to-have — but today's For You is just a heuristic: `content_posts` ordered `created_at desc`, then a stable sort lifting **followed artists first** (`FeedClient` line ~54). No real relevance model. The work: define **signals** — recency, **locality/city** (#90), artist **follows**, **likes/saves**, reel **watch-through / engagement**, **ticket conversions**, **genre affinity**, plus **diversity** so the feed isn't all one artist — and a score that blends **discovery** (surface new local artists) with **relevance** (who you follow / genres you like). Start **simple and explainable** (a weighted score in a ranked query or a server pass), **instrument engagement** to tune against, and guard against a filter bubble by injecting fresh/nearby discovery. **Cold-start** for a brand-new fan = city + recency + popularity (and intro reels, #143, as a seed). Ties hard into **#137** (must stay fast at scale) and reuses the like/follow/attended signals already captured. Not launch-blocking — chronological is fine for the soft launch — but it's the long-term heart of the product. | Real usage data to tune against (needs real accounts live — see launch runway). Sequence after the soft launch. | L (epic) |
| 🟡 | 143 | **Artist introduction reels (a pinned "who I am" reel)** | **Built 16 Aug 2026 — code shipped, awaiting staging verification + prod migration.** An artist can set a short **intro reel** ("this is me, this is my sound") from their own profile — **Add / Replace / Remove** — that pins to the **top of their public profile**, above the shows, so a fan meeting them starts with a face and a sound even when the artist has **no show to promote**. One per artist, replaceable. **Surfacing:** pinned on the profile, AND (added 16 Aug) recent intros now inject into the **For You feed** as "new artist" discovery cards (`FeedIntroCard` → View profile), a stand-in until #142's real cold-start ranking. Reuses the existing content-post + Cloudflare Stream upload path (`uploadContentMedia`) — no new media plumbing. **addendum_038**: `content_posts.is_intro` (a real flag, because `event_id is null` already means "announcement") + one-intro-per-artist partial unique index; the feed excludes intro posts, the profile shows them. Files: `IntroReel` (event-less player), `IntroReelModal`, `profile/intro-actions.ts` (save/replace/remove, service-role), `fetchArtistIntro`, both profile pages, i18n `introReel.*`. Degrades gracefully pre-migration (feed filters in JS; fetch catches the missing column). Build + knip + type-check clean. **Next: run addendum_038 on staging → test → prod.** Complements #115; seeds #142. | Done (profile pin). | M |
| 3 | 144 | **Bizum at checkout (Spanish payment method)** | **Raised 14 Aug 2026 during the Stripe go-live audit.** Checkout is **card-only** today — `payment_method_types: ["card"]` in `src/app/(app)/checkout-actions.ts`. **Bizum** is the dominant consumer payment method in Spain (phone-number instant transfer), so card-only likely leaves conversion on the table for a Madrid audience. It's an **async** method (settles after the redirect), and the fulfilment path already handles that correctly — the webhook only issues a ticket on `checkout.session.completed` **when `payment_status === "paid"`** and also listens for `checkout.session.async_payment_succeeded`/`_failed` (releases the held seat on failure). So the change is small: enable Bizum in the Stripe dashboard (Payment methods) and add `"bizum"` to the session's `payment_method_types` (EUR-only, which we already are). Verify the held-seat release on an abandoned/failed async payment still behaves. **Do after live payments are on (#95)** so it can be tested with a real Bizum. | Live Stripe (#95) to test end-to-end; trivial code change. | S |
| 🟡 | 145 | **Transfer / gift a ticket to another fan** | **Built 14 Aug 2026 — code shipped, awaiting staging verification + prod migration.** One-time **claim link** (Vir's product call): the holder taps **Transfer** on the ticket/QR sheet → gets a link (`/claim/<token>`) to share by WhatsApp/anywhere; the ticket stays theirs with a **"transfer pending"** badge until claimed, and they can **cancel** the link. Recipient opens it (signed-in, or signs up via a `next=` redirect back), taps **Claim** → the ticket moves to their Tickets tab. **Refund-ownership was determined by Stripe, not chosen:** a refund physically returns to the card that paid, so a transfer only ever moves `tickets.user_id` and **never touches `price_paid`/Stripe ids** → a later refund still lands on the original buyer's card. **QR guardrail solved by rotation:** added `tickets.qr_secret` — the QR/Wallet barcode now carries the secret and the scanner looks it up by it; a claim **rotates the secret**, killing the sender's pre-transfer screenshot (residual race bounded by single-scan-wins check-in). Guardrails enforced server-side: un-scanned + not-refunded + upcoming only, one active transfer per ticket (partial unique index), `ticket_transfers` row is the audit trail. **addendum_037** (`qr_secret` + backfill + `ticket_transfers` table + RLS). Files: `saved/transfer-actions.ts`, `claim/[token]/`, `TicketQRModal`, `scan/page.tsx`, `apple-wallet.ts` + pass route, `share.ts` (`shareUrl`), i18n `claim.*`/`ticket.transfer*`. Build + knip + type-check clean; claim page render verified. **Next: run addendum_037 on staging → test round-trip → prod.** Pairs with #149. | Done (claim-link). | M |
| 4 | 146 | **Refund information — buyer-facing policy + status** | **Partial 16 Aug 2026:** the refund policy the buyer saw at checkout (final sale unless cancelled) now also shows **on the ticket** (`TicketQRModal`, `ticket.refundPolicyNote`, en+es) — decision-free, no migration. **Still open (needs Vir's policy call):** a fan-initiated **refund request** flow to admin, and whether the policy is **platform-wide vs per-event**. Original: **Raised by Vir 14 Aug 2026.** *My read:* fans see nothing about refunds until a ticket is already refunded — the QR sheet renders a refunded state, and the mechanics exist admin-side (#43 cancel-refunds, #51 per-ticket), but there's **no buyer-facing refund policy** anywhere. Surface it: at **checkout** and on the **ticket**, tell the buyer whether/when a refund is possible (e.g. refundable until N days before, or "no refunds" — platform default or a per-event setting), and show a clear **status** when one has happened (amount, date, ticket void). Possibly a **fan-initiated refund request** that routes to admin (not automatic). All strings i18n (en+es). | Vir to define the actual policy: platform-wide vs per-event, and self-serve vs request-only. | S–M |
| 3 | 147 | **Payout checks / reconciliation safeguards** | **Raised by Vir 14 Aug 2026, alongside the Stripe go-live audit.** *My read:* as real money starts flowing (#95), add checks so money **out** matches money **in**. Today `/admin/payouts` shows each artist's Stripe balance against their event calendar (release once the show has happened). Harden it: **reconcile** transferred amounts against tickets sold + fees taken (flag mismatches), re-verify the **transfers capability**/`stripe_payouts_ready` before release, refuse to release on a **cancelled/refunded** show, and surface a per-artist **owed-vs-paid** audit (optionally anomaly alerts). Overlaps #57 (admin metrics) and the money-integrity concerns in the go-live audit. | Confirm scope: reconciliation dashboard vs automated release guards vs both. | M |
| 5 | 148 | **Report a cancelled / wrong show (listing hygiene)** | **Raised by Vir 14 Aug 2026.** *My read:* external/imported listings (#111/#118) are shows MadGigz doesn't control, so they go stale — cancelled, rescheduled, sold out, wrong info. Let a fan **flag "this show is cancelled / wrong"** from the event page, routing to the **admin queue** (reuse the content-report / feedback plumbing, #94/#97) so a human verifies and hides/fixes it — keeps listings trustworthy once real events are seeded. Distinct from an artist/admin **cancelling a house show** (that already soft-cancels + refunds every ticket via `cancelEvent`) — this is a **user-reported** signal on listings we didn't create. *Alt reading:* if it means "**notify ticket holders** when a show they hold is cancelled," `cancelEvent` refunds them but check whether they get an explicit notification (#86). | Confirm which reading (user-reported stale listing vs holder cancellation notice). | S–M |
| 8 | 149 | **Friends — a fan-to-fan social layer** | **Raised by Vir 14 Aug 2026.** A **friends graph between fans**, distinct from following artists (#60): add friends, see which shows friends are **going to / have been to**, and **"who else is going"** on an event — so buying a ticket becomes social (go together). Feeds discovery and the algorithm (#142) with a strong signal, and pairs with **ticket transfer** (#145 — send a spare to a friend). Big feature: friend **requests/accept**, a **privacy model** (who can see your going/attended lists), a friends list on the profile, and surfacing "friends going" on events. | Product design — privacy model (mutual vs one-way, visibility) and where friend activity surfaces. | L |
| ✅ | 150 | **Separate ticket row + QR per seat** | **Shipped 14 Aug 2026, verified on staging + prod.** Buying N tickets now creates **N ticket rows** (quantity 1, `seat` 1..N), each with its own id → own QR + own wallet pass, instead of one row with quantity N. `fulfil_ticket` (addendum_036) loops and splits price/fee/VAT exactly across seats (remainder cents on the first seats, so the rows sum back to the order total); a new `seat` column + unique `(stripe_session_id, seat)` index replace the per-session one. **No UI change needed** — `SavedClient` already renders one card/QR/pass per ticket row. **Refunds became per-seat partial refunds** (each row refunds its own `price_paid`; `reverse_transfer`/`refund_application_fee` act proportionally to amount); pre-migration single rows hold the whole order, so the code degraded gracefully before the SQL ran. **Migration run on prod + staging 14 Aug.** Verified end-to-end: 3 separate tickets, "Where you've been", "Already checked in", per-seat refund all check out. **Unblocks #145** (transfer one of N) and per-pass wallet. | Done. | M |
| 🟡 | 151 | **Multiple price tiers per show (each with a sold-cap and a cutoff)** | **Built 16 Aug 2026 — code shipped, awaiting staging verification + prod migration.** Simultaneous model (Vir's call): a show can offer several tiers at once (General €12 / VIP €20), each with its own price, sold-cap and optional on-sale-until cutoff; the fan picks one in the ticket sheet. **Additive & optional** — a show with no tiers is unchanged (single events.price). **addendum_039**: `event_tiers` table + `tickets.tier_id`; `reserve_tier_capacity`/`release_tier_capacity` (bump the tier AND the events aggregate, guarded by cap+cutoff+live — same atomic discipline as reserve_event_capacity); `fulfil_ticket` gains `p_tier_id` (recreated, old 10-arg dropped). **Money path is fully tier-aware**: checkout picks/validates the tier server-side and prices off it, the webhook releases the tier on abandon/fail, and both refund paths (cancelEvent + refundTicket) release the tier. For a tiered show events.capacity=Σ caps and events.price=min tier price, so every existing sold-out/almost-gone/metrics read stays correct. **Fan UI**: tier picker in `TicketModal` (sold-out/expired tiers disabled). **Admin**: `TierManager` on the event detail page (add/edit/remove; can't remove or shrink below a tier's sales). **Artist self-serve tiers added 16 Aug:** the artist manages tiers on their own show via `ManageShowModal` (localized `ArtistTierEditor`, owner-checked `saveArtistTiers`), with a **per-tier fee breakdown** (`FeeBreakdown` per row). Shared write core `src/lib/tiers-apply.ts` (admin + artist both call it). **Create-form reshape (16 Aug, addendum_040):** on **add-show**, pricing IS the ticket-type editor (always ≥1 type; a single-price show is one type) — the standalone Price + event max-per-order fields are gone; **Capacity moved above**. Each type now carries **Available** (per-type qty) and **Max per order** (`event_tiers.max_per_order`, e.g. max 2 VIP / 4 General) — checkout enforces the chosen type's cap, the fan stepper honours it. `events.capacity` stays the artist's room total (types validated to sum ≤ it); `events.price` = cheapest type. External shows keep the single price field. Degrades gracefully pre-migration (tier reads catch the missing table). Build + tsc + knip clean; single-price render verified no-error with the table absent. **Next: run addendum_039 on staging → test tiered buy + refund → prod.** | Product call made (simultaneous). Artist self-serve UI deferred. | L |
| ✅ | 82b | **Apple sign-in** | **Shipped to prod 12 Aug 2026** — the day Apple approved the developer verification. Apple provider configured in Supabase (Services ID `es.aurasonic.madgigz.web`, `.p8` key, return URL on the new custom domain `auth.aurasonic.es/auth/v1/callback`); `AppleButton` added (mirrors `GoogleButton`, provider `apple`, inlined monochrome Apple mark), placed **above** Google on signin + signup per Apple's HIG, flowing through the same provider-agnostic `/auth/callback` 82a built. Verified: authorize endpoint redirects to `appleid.apple.com` with the right client_id + callback. Strings `signin/signup.withApple` (en+es). **Maintenance:** the Apple client secret is a JWT that expires ~180 days — see [[125]] (rotate ~Feb 2027) or it silently breaks. | Done. | S |
| ✅ | 111 | **Mass gig upload / gig-database import** | **Shipped 13 Aug 2026.** New admin importer at **`/admin/events/import`** ("Import gigs" button beside "New show"): paste a spreadsheet (TSV) or CSV, **Preview** validates every row and writes nothing, then **Import** creates only the rows previewed as ready. Imported shows are the #62 shape — `artist_id` null, `ticketing_mode='external'`, `house_run=false`, city Madrid, accent rotated across the brand swatches. Column mapping is alias-tolerant (title/artist/venue/date/ticket_url required; time/price/genre/age/description/image/capacity optional; "Billed as", "Ticket Link" etc. all map). Dates read ISO or **day-first DD/MM/YYYY** (Spanish); prices strip €/commas; **dedup key = title+venue+date** catches both re-uploads (against existing `events`) and within-batch dupes; venues **create-or-link** via the shared `resolveVenue` (no "Sala But"/"sala but" splits). Architecture: pure, unit-tested core `gig-import-parse.ts` (26 assertions in `scripts/test-gig-import.mjs`) + thin server action `gig-import.ts` that adds only DB dedup + insert — **one code path for preview and commit** so they can't drift (the client re-sends the text, never trusted parsed rows). Admin panel stays English (no i18n). Build/lint/knip clean. **Feeds #118** (Bandsintown seeding reuses this importer). **Admin-eyeball pending** — needs Vir's admin login to run a real paste (Preview is safe/no-write, so low risk). | Done. | M |
| 2 | 118 | **Seed real Madrid shows from Bandsintown** | Populate the app with genuine upcoming gigs by pulling Bandsintown listings **filtered to our `venues`** (Sala But, La Riviera, Caracol, El Sol, Siroco, Clamores, WiZink…), capturing what a listing carries: **external ticket link, description, line-up, price, date/time, poster**. These land as **external-ticketing events** (`ticketing_mode = 'external'`, no payout/checkout — the existing admin-created + external-link model, #62), owned by no artist, city = Madrid (#90). Ingestion should **reuse the #111 mass-import path** (per-row validation, venue create-or-link, dedup key, dry-run preview), not a second bespoke pipeline. **The real decision is the source, and it's not a coding one:** Bandsintown has an **official API** (partner programme with its own terms) — that's the sanctioned route; scraping the site likely breaches their ToS, and re-hosting their posters/descriptions raises copyright (a ticket **link** is just a URL, fine to store). So prefer the API, attribute the source, and treat descriptions/images carefully. Needs a refresh/expiry story so listings don't go stale. | Bandsintown API access (or another ToS-safe source) + the #111 importer. | M |
| ✅ | 113 | **Move Log Out + Delete account into Settings** | **Shipped 12 Aug 2026.** On both fan and artist profiles, Log Out and Delete account moved off the profile body into the Settings sheet (`ProfileClient.tsx`) — Log Out a normal row, Delete account the quiet underlined control at the bottom; opening Delete closes Settings first so the dialog isn't stacked. | Done. | S |
| ✅ | 114 | **Show posters in the artist's shows lists** | **Shipped 12 Aug 2026.** The artist profile's "Your Shows" rows (`ShowRow`) — and the "Tagged in" rows — now lead with the show's poster thumbnail, so an artist recognises a show by its art instead of reading titles. | Done. | S |
| 4 | 115 | **Flesh out the fan profile** | Today a fan profile is just two stat numbers (Attended / Saved) — it feels empty. Give it real content. Leading candidate is the past-events poster wall (#116); could also surface followed artists and liked shows. **Vir to say what a fan should see there** — #116 is the concrete first piece. | Vir to pick the content. | S–M |
| 4 | 116 | **Past events as a poster wall (DICE-style)** | **Shipped 13 Aug 2026.** The fan profile now shows a **poster grid of the shows the fan was scanned in to** (DICE "memories"), newest first, below the Attended/Saved stats — the first real content on an otherwise-sparse fan profile (first piece of #115). New `fetchAttendedEvents(supabase, userId)` mirrors the proven `fetchTaggedShows` FK-embed (`tickets → events(*, venues(address))`), filtered to `checked_in_at not null` + `hidden_at null` so the wall matches `attendedCount`, de-duplicated (two tickets to one show = one memory), sorted newest-first. Rendered in `ProfileClient` as a 3/4-aspect poster grid (3-col mobile, 4-col ≥sm) with a legibility gradient + title/date; each tile links to the public `/e/[id]` page. Fan-role only, and only appears when there's ≥1 attended show (no empty shelf for new fans). Strings `profile.pastShowsTitle`/`pastShowsSubtitle` (en+es). Build/lint/knip clean. **Visual eyeball pending** — needs a fan account with a scanned-in ticket to see it populated. | Done. | S–M |
| 8 | 117 | **Raise upload size / compress videos** | Now on Supabase Pro (~100GB storage / 250GB egress), the per-file cap (`MAX_CONTENT_FILE_BYTES = 50MB` in `src/lib/media.ts`, plus the bucket's `file_size_limit`) can be raised. **The real cost is egress, not storage** — every feed view re-streams a video, so a bigger file is paid for on every play and on fans' mobile data, which is why "just raise it" isn't free. Recommendation: bump the limit **modestly** now (cheap, unblocks bigger clips), but real **video compression** (transcode/downscale on upload) needs WebCodecs client-side or ffmpeg server-side — that's the **#99 in-app video editor**'s media pipeline, not a quick constant change. Images are already downscaled (#96); video is the gap. | Nothing to raise the cap. Compression rides with #99. | S (cap) · L (compression) |
| ✅ | 104 | **Claude skills for code organisation** | **Shipped 12 Aug 2026.** Four convention skills now live in `.claude/skills/` alongside `simulate-users`: **`db-migration`** (numbering, the public/owner/service column classification + column GRANTs, the two-phase revoke, the drop-policy-name trap, graceful degradation, the fresh-DB setup file, staging-then-prod, never `drop schema public`), **`i18n-string`** (en.ts → es.ts → `export-i18n-json.mjs` → review PDF, admin-English, `{var}` interpolation), **`adversarial-probe`** (the `scripts/*probe*.mjs` set + the read-the-value-back rule), and **`next-conventions`** (read the bundled Next docs, no module-scope env throw, no `useSyncExternalStore`, the setState-in-effect ban + escapes, realtime `setAuth`). Each is an invokable checklist grounded in the real files/scripts, so the tribal knowledge in CLAUDE.md is now applied the same way every time. | Done. | S |
| 7 | 105 | **Full web / desktop version** | **Foundation pass shipped to prod 11 Aug 2026** (TikTok-web model). At `lg` (≥1024px), the mobile phone column gives way to a real desktop shell — mobile is untouched below `lg`. What shipped: a responsive `(app)` shell (`layout.tsx`); a persistent left **`SideNav`** (wordmark + Feed/Explore/Tickets/Notifications/Profile, active states, live unread pill) replacing the bottom nav on desktop; a shared `nav-icons.tsx` so the two navs can't drift; the For You reel as a **centred, fully-visible 9:16 card** height-clamped to `min(100%, 26rem*16/9)` so it never re-crops as the window resizes (the original clip complaint); **desktop up/down arrow buttons** that snap one reel per click alongside trackpad scroll (`feed.previousReel/nextReel`, en+es); and sensible wide-screen widths elsewhere (Explore 4-col grid + max-w-5xl; Tickets/Profile/Notifications centred max-w-2xl). **Deferred follow-up (deep redesign):** bespoke per-tab desktop layouts — masonry/multi-column Explore, two-column profile, feed side-rails/next-up previews. Also still open: whether a separate **marketing site** on the apex `aurasonic.es` is wanted (that's the Odoo site's job, not this). | Deep redesign later, Vir's call. | M–L |
| 3 | 95 | **Go live — real payments (domain already shipped)** | **The domain is live (11 Aug 2026):** `madgigz.aurasonic.es` serves the app, `NEXT_PUBLIC_APP_URL` + Supabase redirect URLs are set, `/api/health` is green. **Stripe stays in test mode** for this soft launch — real signups/shows/browsing, test-mode checkout. What's left is the real-payments flip: live Stripe keys, a live webhook on the new URL + its signing secret, artists completing real Connect onboarding, and the IVA/tax question (#97) settled first. Runbook in `docs/go-live.md`. | Vir — flip when tax + payouts are ready. | M |
| ✅ | 108 | **Pre-production / staging environment** | A second, hosted copy of the app to test big changes against a realistic stack *before* they touch live — because once #95 ships, "test it locally" stops being safe enough. A `staging` (or `preview`) Vercel deployment on its own subdomain, pointed at a **separate Supabase project** (its own DB, its own migrations run first) and **Stripe test mode**, driven by a `.env.staging`. Then the flow becomes: change → deploy to staging → exercise it (the `simulate-users` skill and the probes already take `--env=.env.staging`) → promote to prod. Ties into #103 (staging holds its own set of the same secrets) and gives the user-simulation harness the throwaway project it always wanted. | **Shipped 11 Aug 2026.** Locked staging Preview live at `madgigz-app-git-staging-aura-sonic.vercel.app` (Vercel Authentication) on its own Supabase (`hzuqhraoslgjibdcccyx`) + Stripe test; Google sign-in configured, Turnstile off on staging. One-paste `supabase/staging_full_setup.sql`, runbook `docs/staging.md`, and `/api/health` `supabaseHost` isolation check all in place. Lesson banked in the runbook: **never `drop schema public` to reset a Supabase project** — it wipes default grants and silently breaks writes; recreate the project instead (the first staging DB was rebuilt for exactly this). **Remaining (optional):** the Stripe test webhook needs a Protection-Bypass token to get past the login wall — do it when payment edge-cases need testing. | M |
| 4 | 90 | **City-centric — the app's core identity** | MadGigz is *local*: a fan sees shows in **their city only**, framed as "You're in Madrid — here's what's on". Launch is **Madrid only**, then cities are switched on **one at a time**. Groundwork is already there — `events.city` and `venues.city` both exist (default `'Madrid'`). The work: a "current city" the app knows (a launched-cities list + how the user's city is set — see note), feed/Explore/This-Week filtered to it, and the city named in the UI. **Later:** a fan can switch cities (travelling), and a touring artist can post shows in another city (venue picker not locked to one city). | **#90a shipped 11 Aug 2026:** the local-first framing — `src/lib/city.ts` (`CURRENT_CITY`), a single-city filter on the feed + Explore, and a "📍 Madrid" pill (en/es). **#90b remaining (deferred until city #2):** launched-cities list, per-user current city, and city switching — the multi-city machinery. | L (90b) |
| ✅ | 101 | **Live updates (Supabase realtime)** | **Shipped to prod 11 Aug 2026** (verified live on staging: two-account tests moved the bell dot and the ticket-sheet sold bar with no reload). `src/lib/realtime.ts` — `useLiveUnreadCount` (the profile-tab bell dot) and `useLiveEventStats` (the ticket sheet's sold bar / "Almost gone" / sold-out lock), wired into `BottomNav` and `TicketModal`, both seeded from the server-rendered value (no flash) and degrading to reload-to-refresh if realtime is off. `supabase/addendum_033_realtime.sql` adds `notifications` + `events` to the `supabase_realtime` publication (RLS unchanged — no new read access) and sets `notifications` to `replica identity full`. **The non-obvious fix:** the `@supabase/ssr` browser client doesn't push the cookie session to the realtime socket before subscribe, so the recipient-scoped bell got nothing — the hooks now `getSession()` + `realtime.setAuth()` before subscribing. Scope was deliberately the two numbers that mislead when stale, not "realtime everywhere". **Live on prod 11 Aug 2026** — `addendum_033` run on the prod Supabase, so the bell dot and the ticket-sheet sold count now push live for real users. | **Done.** | M |
| 6 | 110 | **Get on the app stores (Play Store first, App Store later)** | **Google Play developer account verified 13 Aug 2026** — identity/verification cleared, so the Android side is now unblocked all the way to submission (build the TWA → AAB → store listing whenever ready; not urgent, do it before a marketing push). **Android TWA groundwork shipped 13 Aug 2026 (my parts):** the **web-facing account-deletion page** Google Play requires is live at **`/delete-account`** (public, bilingual, mirrors the real 30-day-grace/scrub-not-delete/tax-retention behaviour of `account-deletion.ts`; links to in-app Settings + an email fallback), and the **`/.well-known/assetlinks.json`** route is built and env-gated (`ANDROID_PACKAGE_NAME` + `ANDROID_CERT_FINGERPRINTS`, comma-separated) — returns `[]` until set, the real entry once the values are in, no code change needed. Verified public (middleware only refreshes tokens, both routes sit outside the authed `(app)` group). **Remaining for Android (Vir):** run **Bubblewrap/PWABuilder** to generate the TWA + signing key → get the **package name + SHA-256 fingerprint(s)** → set those two env vars on Vercel → build the signed **AAB** → Play **Internal testing** → install on the new Samsung A07. Test device acquired 13 Aug 2026. **TWA built + running on the A07 13 Aug 2026:** Bubblewrap set up by hand (its JDK downloader is broken on Apple Silicon — it fetched only the JDK *source*, never the binary, so `config.json` never saved; fixed by installing Temurin 17 aarch64 + Android cmdline-tools `6609375` + build-tools 36.1.0/platform-36/platform-tools into `~/.bubblewrap`, writing `config.json`, accepting licences → `doctor` green). `init` (package **`es.aurasonic.madgigz`**, portrait, `#0a0807`) → `build` → signed APK + AAB. Upload-key SHA-256 (`9A:49:…:D6`) set as `ANDROID_CERT_FINGERPRINTS` + `ANDROID_PACKAGE_NAME` on Vercel Prod → **live `assetlinks.json` verified** (URL bar drops). APK installed via `adb` on the A07 — buying a ticket + Add-to-Wallet confirmed working on device. **Device-testing fixes (13 Aug, web bundle — appear in the app on the next prod deploy, no reinstall):** (1) Explore was showing **past** shows (Hard Fuse Aug 10) — added `upcomingOnly` to `fetchEvents`, Explore now `event_date >= today`; the "on top on Samsung only" was just the followed-artist float, not a device bug; (2) reel feed scrolled **loosely on Android** (a flick skipped several reels) — added `snap-always` (`scroll-snap-stop: always`) so each swipe stops on the next reel. **Still Vir-side for a store release:** the Play **signing key is the local upload key** — once uploaded, add Google Play App Signing's SHA-256 as a *second* fingerprint (route already takes comma-separated); then Play **Internal testing** for a Store-installed build. **PWA groundwork shipped 11 Aug 2026:** `public/sw.js` (conservative service worker — offline fallback, never caches authed HTML), `public/offline.html`, production-only registration, `/sw.js` headers, middleware exclusions. Verified installable (SW registers, scope `/`, activated). So the app is now installable + wrappable. **iOS shell (Capacitor) built 12 Aug 2026 — internal-TestFlight path:** because MadGigz is server-rendered (no static export), the app is a **Capacitor 8 shell that loads the live site** (`server.url = madgigz.aurasonic.es`) — no CocoaPods (Capacitor 8 uses SPM). Native OAuth bridge solves the webview-Google block (`disallowed_useragent`): `startOAuth`/`isNativeApp` (`src/lib/native.ts`) open Google/Apple in the **system browser** and return via a `madgigz://auth-callback` deep link that `NativeBridge` (mounted in the root layout) forwards to the existing `/auth/callback` — reusing the web routing, no duplication. Web path unchanged (guarded on `isNativeApp()`, verified: Apple/Google/email login all still work on web). Info.plist carries the `madgigz` URL scheme, `NSCameraUsageDescription` (QR scanner), portrait-lock; icon + branded splash generated from brand assets via `@capacitor/assets`. **Verified building + running:** `xcodebuild` succeeded (82s), the app launches in the iOS Simulator and loads the live landing/sign-in natively. **Chose internal-TestFlight first** (no Apple review) — Apple 4.2 rejects thin wrappers for the *public* store, so the native-feature work (push, Universal Links, native Sign in with Apple, etc.) is a deliberate later phase. **Remaining for a TestFlight build:** (you) add `madgigz://auth-callback` to Supabase Redirect URLs; (you) set the Xcode signing Team + create the App Store Connect record; Archive → upload → Internal Testing. **The site must be deployed with the native-bridge code for OAuth to work in the app** (the app loads prod's JS). **Still to do (public store):** It's a web app, so **wrap it, don't rebuild.** **Android / Google Play:** a TWA (Trusted Web Activity) — an Android shell around the PWA via PWABuilder or Bubblewrap → a signed AAB; needs `/.well-known/assetlinks.json` (Digital Asset Links) to verify the domain and drop the browser URL bar. **iOS / App Store:** harder — Apple guideline 4.2 rejects thin web wrappers, so it wants a Capacitor shell with enough native feel; waits on the Apple Developer account (#82b). **Already have:** web manifest (`src/app/manifest.ts`), full icon set incl. maskable, HTTPS, and in-app account deletion (Play requires it). **Gaps:** no **service worker** yet (installability + offline — `next-pwa` or hand-rolled); the Digital Asset Links file; a **public privacy-policy + web account-deletion URL** (Google now requires a web-facing deletion page, not just in-app); and store listings (screenshots, feature graphic, descriptions ×2 langs, content rating, Google Data-Safety / Apple privacy labels). **Fees:** Play Console **$25 one-time**, Apple Developer **$99/yr** (already needed for #82b). **The one real gotcha — payments:** the stores demand their 30% in-app purchase for *digital* goods, but **live-event tickets are a real-world service** (the Eventbrite/DICE exception), so Stripe checkout should be allowed — must be confirmed against current store policy, because a misclassification is a hard blocker. | Android: nothing technical (PWA service worker + the store paperwork). iOS: the Apple account (#82b). Not urgent for the soft launch — the PWA already installs via "Add to home screen"; do this before a marketing push, for discoverability + trust. | M (Android) · L–XL (iOS) |
| 7 | 88 | **Promoter & venue flows** | Account types alongside fan/artist, probably web rather than the app. Groundwork exists: admin-created shows already model a show with no `artist_id` managed by its creator, `venues` rows carry a `verified` flag an account could claim, and the artist claim-and-evidence flow is the precedent for verifying someone represents a venue. | Later, your call. Decide ownership first. | L |
| 8 | 92 | **Band infrastructure (artists ⇄ bands)** | **Artists are people, bands are groups**, and the link is **many-to-many**: an artist plays in several bands, a band has several artist members. So a band is its own profile-like entity (name, photo, socials, its own shows), joined to artist profiles through a `band_members` table (band_id, profile_id, role, share) — not a flag on a profile. Members keep their own artist accounts and appear under each band they're in; a band's shows can also surface on each member's profile. `event_artists` (`addendum_012`) is the precedent for profile↔event tagging, but this is profile↔band↔profile, a new entity and a new answer to "who owns this". Big feature, genuinely useful. The sharp edge is money, not the graph — see below. | Not now, by Vir's call. Design the payout model first. | XL |
| ✅ | 58 | **Admin activity tracking** | **Shipped 13 Aug 2026** (scoped by Vir: login timestamps, revenue, events attended, events liked — geolocation explicitly dropped). Turned out to need **no new tracking table**: the per-user admin page (`/admin/users/[userId]`) already showed **Last sign-in** (from `auth.users.last_sign_in_at`), **Joined**, **Spent**, **Attended**, **Tickets bought**, **Following** — all derived from existing `tickets`/`auth`. The one gap from Vir's list, **events liked**, is now a `saved_events` count (`likedCount` on `AdminUserDetail`) shown as a headline stat card beside Spent/Attended. The users **list** already surfaces Joined + Last sign-in for scanning login recency. No migration; build/lint/knip clean. **Optional fast-follow (Vir's "we can even"):** capture **hometown** via an onboarding question (`profiles.hometown` column + signup UI in en/es + admin display) so cities are visible — a mini-feature, not built in this pass. | ✅ core done; hometown optional. | L |
| 10 | 99 | **In-app video editor** | Trim, crop to vertical, add a music bed, maybe captions — so an artist can post from their phone without leaving for CapCut and coming back. Real scope: it is a media pipeline, not a screen. Browser-side trimming is doable with the WebCodecs API; anything more (music beds, transcode-on-upload) wants server-side ffmpeg, which Vercel's function limits make awkward. Music also needs a licensed library, which is a commercial deal, not code. | Nothing technical. Worth doing once artists are actually posting. | XL |
| 11 | 100 | **Supabase storage and bandwidth headroom** | Vir is happy to pay — this is about noticing *before* uploads start failing, not about avoiding the bill. Free tier is 1GB storage / 5GB egress; Pro is $25/mo for 100GB. The real lever was #96 — **shipped 11 Aug 2026:** `src/lib/image-resize.ts` downscales images (long edge → 1920px, re-encoded) inside `uploadEventMedia`, so posters, avatars and content photos shrink client-side before Storage (a 4000×3000 test image went 432KB → 133KB). Cuts both storage and egress. Still wants a size figure in the admin dashboard so the trend is visible. **Update 11 Aug 2026: Vir upgraded Supabase to Pro** — ~100GB storage / 250GB egress included, so the ceiling is far off; the per-file **upload size limit can also be raised now** (a bucket `file_size_limit` plus the client-side check, e.g. for artist video posts feeding #99). Drops to a low-priority watch + the admin size figure. **Admin size figure built 11 Aug 2026 (on `staging`):** the admin dashboard now has a **Storage** panel — total used vs the ~100GB Pro quota with a headroom bar, plus a per-bucket breakdown (files + size for `event-media`, `artist-evidence`). Powered by `supabase/addendum_034_storage_usage.sql` — a service-role-only `admin_storage_usage()` RPC that aggregates `storage.objects` (that table's in the `storage` schema, which PostgREST can't reach directly). Egress/bandwidth is a billing-only metric with no SQL source, so it stays in the Supabase dashboard; this is the file footprint, the part that only grows. Degrades gracefully (shows a "run addendum_034" note) until the migration runs. Verified on staging (addendum_034 run there); **code promoted to prod 11 Aug 2026**. **Done 12 Aug 2026: `addendum_034` run on prod**, so the admin Storage panel now shows real numbers. | ✅ Done. | S |
| ✅ | 109 | **Surface upload errors in the artist claim form** | **Shipped 11 Aug 2026.** `ArtistClaimForm.tsx` now wraps `uploadArtistEvidence` in try/catch — a rejected upload shows the error and logs the reason instead of leaving the button stuck on "submitting…". The silent-throw that masked the staging grant issue is gone. | Done. | S |
| 12 | 97 | **Tax & invoicing (facturación) — Odoo-backed** | Real euros mean IVA on the MadGigz fee, facturas artists can hand their accountant, and ticket-sales invoicing for house shows. **Refined 11 Aug 2026 (Vir, planning ahead — not building until the gestor signs off):** Odoo is the **invoice engine** (Invoicing app live, Spanish PGCE chart, "MadGigz service fee" 21%-IVA product, RPC via `~/AuraSonic Ops/odoo/odoo.py`); MadGigz's job is **capturing fiscal identity + organisation data**, never generating a legal factura itself. Two distinct invoice flows and an open "where does capture live" fork — see the design note below. **Gestor signed off 12 Aug 2026:** intermediation IVA structure valid (21% on the 5% commission only, provided the sale contract stays organiser↔fan); **monthly consolidated commission invoice per organiser** is fine ("Comisión por intermediación en venta de entradas [mes/año]", X × €0.50 base + 21% IVA); **Veri*factu applies from invoice #1** (Odoo module `l10n_es_edi_verifactu` available uninstalled — needs the company certificado digital); no Modelo 238; Modelo 347 threshold to re-confirm (gestor said 600k€, the usual figure is €3,005.06/client/yr). **Lawyer requirements added 13 Aug 2026 — the capture spec:** organisers must issue their own invoices to their customers (now in the Organiser Terms); MadGigz must **collect organisers' tax info before payouts** — EU-established: VAT ID (NIF in Spain); non-EU: government-issued ID. Fan invoicing by MadGigz not required, but worth designing into the flow as optional receipts. **Fiscal-identity capture BUILT 19 Aug 2026 (the lawyer's "collect tax info before payouts" half).** `addendum_042` adds six **service-role-only** columns to `profiles` (`fiscal_legal_name`, `fiscal_id`, `fiscal_id_type` nif/vat/other, `fiscal_country`, `fiscal_address`, `fiscal_provided_at`) — sensitive, deliberately **not granted** (same treatment as `stripe_account_id`/`date_of_birth`), read/written only via the service-role client from session-authed server actions. New `FiscalIdentityCard` sits beside `PayoutCard` in the artist Settings sheet (collapsed status row → form; values load lazily, never shipped with the page); pure `src/lib/fiscal.ts` (normalise + permissive validation, 8 unit tests) + `src/lib/fiscal-server.ts` (DB layer, catches `42703` so it degrades to "not provided" pre-migration) + `fiscal-actions.ts` (save/load own). Admin **Payouts** page now shows each organiser's tax details for invoicing, or a red **"⚠ No tax details on file — collect before releasing"** warning. i18n `fiscal.*` (en+es). `npm run check` clean. **Next:** run `addendum_042` on staging → prod; then the **monthly commission-invoice run** through Odoo (still needs the certificado digital for Veri*factu). | Certificado digital for Veri*factu; monthly invoice run. Capture flow now built. | L |
| 12 | 112 | **Sync registered users into Odoo CRM** | Push MadGigz's registered users into Odoo as contacts (`res.partner`) so the CRM/marketing base lives in one place, via the RPC tool `~/AuraSonic Ops/odoo/odoo.py`. Source: Supabase `profiles` (username, role, created_at) joined to each `auth.users` email (service-role, server-side). Dedup on email. Decisions: a one-off **backfill** of existing users, plus an **ongoing sync on each new signup** (a hook/trigger), ideally both; and it **shares the `res.partner` model with #97's fiscal identities** — the two must reconcile to one contact per person, not duplicate partners. **GDPR:** names/emails are personal data, so moving them to Odoo for marketing needs a lawful basis (and Odoo becomes another processor); account deletion (`src/lib/account-deletion.ts`, `addendum_019`) must also remove/anonymise the Odoo contact, or the sync must be a deletion-aware one-way mirror. See [[odoo-instance]] and the #97 design note. | Lawful basis for marketing + backfill-vs-ongoing decision. | M |
| 13 | 98 | **In-house payments** | Replacing Stripe Connect with direct payment handling. **Much later, by Vir's note.** Would mean becoming a payment facilitator: PSD2/SCA, PCI scope, holding other people's money, and a licence. Stripe's 1.5% + €0.25 buys all of that. | Far future. Only worth it at real volume. | XL |
| 3 | 119 | **Terms & conditions (+ the legal pages)** | **Mostly shipped 12 Aug 2026.** The legal set lives on the Odoo site, bilingual: **`aurasonic.es/terms-of-service`** (general — organiser as seller, MadGigz as intermediary "en nombre y por cuenta ajena", the AuraSonic/MadGigz house-show carve-out where AuraSonic is the seller, refunds, no 14-day withdrawal per art. 103.l LGDCU, funds-custody-with-Stripe), **`/organiser-terms`** (organiser = venue/promoter/artist: 5% + €0.25 min + 21% IVA, monthly invoice, payout released after the show, own-taxes + DAC7 clause, cancellation shortfall top-up, verification-isn't-curation), and the existing **`/privacy`** — structure per the gestor's written answers (11 Aug). The app links them at the binding moments (`components/legal/LegalNotice.tsx` + `lib/legal.ts`, locale-aware URLs): signup + signin ("by continuing you agree…"), the artist claim form + switch-to-artist confirm (Organiser Terms), the ticket sheet's final-sale line (now names the organiser as seller + links the Terms — what makes the withdrawal exclusion bind), and a permanent Legal row in Settings. **Lawyer greenlit 13 Aug 2026**, with additions all shipped same day: **`/legal-notice`** (Aviso legal, LSSI art. 10 — registry details still to complete) and **`/cookie-policy`** (technical-cookies-only, AEPD-guideline framing, no banner needed) published bilingual + footer-linked + in the app's Settings legal row; Organiser Terms §4 extended (organisers issue their own customer invoices; EU VAT ID / non-EU government ID required before payouts — the capture build is #97); Google-Fonts import removed from the site theme (DM Sans self-hosted) so no visitor IPs go to Google. **Third-party hole review done + fixes shipped 14 Aug 2026** (bilingual, live): privacy now discloses **Cloudflare** (Stream video hosting + Turnstile at signup) and **Apple/Google Wallet** passes; cookie policy gained a **Turnstile** section; ToS §7 reworded to "instructs Stripe to withhold" (no longer contradicts §10 funds-custody). Policy decisions (Vir, 14 Aug) drafted into both terms docs: **rescheduled show = ticket valid + 14-day full-refund window**; **transfer free / resale for profit banned, first QR scan wins** (new ToS §§8–9, later sections renumbered +2); **payout released 7 days after the show**; **no-show reportable 14 days, MadGigz may declare cancelled + refund, organiser must return released payouts** (Organiser Terms §5–6, renumbered +1). **Still open:** the lawyer's formal review pass of all five texts against LSSI/GDPR/LOPDGDD + AEPD cookie guidance (in progress) — now also covering the holes the 14 Aug review left for legal drafting: **user-content indemnity** (music rights — likeliest real claim), **organiser warrants licences/permits/insurance + is 18+**, **ADR statement** (Ley 7/2017), and privacy-policy **legal bases + international transfers + concrete retention periods** (GDPR art. 13 gaps); stored acceptance versioning (`accepted_terms_at` + re-consent) — deferral trigger has now arguably fired: the 14 Aug resale ban is a new user restriction. | Lawyer pass on the wording; acceptance versioning when terms first change materially. | M |
| 7 | 120 | **Desktop: announcements panel beside the feed, triggered from the sidebar** | **Shipped to prod 12 Aug 2026.** On desktop the "From MadGigz" announcements moved off the bottom-sheet overlay: a first-class **`SideNav`** rail row (megaphone, between Feed and Explore) opens them as an **in-flow column to the left of the feed** — its own scroll + a × close button (no backdrop to tap) — which re-centres the reel in the remaining width, side by side rather than over the top. It reuses the URL-driven `?panel=announcements` state (#102), so the rail item is just a `Link`. Caught + fixed a clip: when the column is open the reel track narrows, so the prev/next arrows hold back to `xl` (they'd clip the viewport edge at `lg`); trackpad scroll is unaffected. **Mobile untouched** — header megaphone + bottom sheet stay (right for a phone). Mobile sheet and desktop column share one `AnnouncementsList` so they can't drift; new strings `feed.announcements` / `feed.closeAnnouncements` (en+es). | Done. | S–M |
| 7 | 121 | **Desktop: add-content panel opens on the right of the feed (mirror #120)** | The "+" post-an-update flow (`AddContentModal`) currently opens as a **centred web dialog** on desktop (shipped 12 Aug 2026 — cleaner than the old bottom sheet). Vir wants to go further: on desktop, open it as an **in-flow column on the *right* of the feed**, the mirror image of how #120 opens announcements on the left — so the reel re-centres and the post form sits beside it rather than over it. Reuse #120's pattern exactly: an in-flow `<aside>` (right border instead of left), its own scroll + × close, the reel arrows holding back to `xl` when it's open (the track narrows on that side too — the arrows live on the right, so watch the collision carefully; may need to hide them entirely when this panel is open). Keep the top-right "+" trigger. **Mobile untouched** — keeps the bottom sheet. Open question: with the panel URL-addressable (a `?compose=1`-style param, like `?panel=`) both the left announcements and right compose could theoretically be open at once — decide whether they're mutually exclusive. | Nothing — pure frontend, same pattern as #120. | S–M |
| ✅ | 122 | **Google OAuth: publish to Production + brand the sign-in domain** | **(b) Brand the redirect host — SHIPPED to prod 12 Aug 2026.** The sign-in screens no longer read *"to continue to `rxtiagsypwvuyyihbhal.supabase.co`"* — the **prod** Supabase project now has a **Custom Domain** (`auth.aurasonic.es`, ~$10/mo add-on): CNAME on GoDaddy → Supabase Activate → Google OAuth client redirect URI added (`https://auth.aurasonic.es/auth/v1/callback`, old one kept) → `NEXT_PUBLIC_SUPABASE_URL` flipped to the custom domain (**Production scope only** — Preview stays on staging's Supabase, #108) → prod redeployed (`NEXT_PUBLIC_*` is build-time inlined, so the client bundle only switches on rebuild; server routes flip at runtime). Verified: `/api/health` `supabaseHost: auth.aurasonic.es`, authorize endpoint returns `redirect_uri=…auth.aurasonic.es/…`, app + REST 200 over the custom domain. Media-URL→path converters made host-agnostic (`storage.ts`, `account-deletion.ts`) so pre-cutover objects still resolve. Runbook in `docs/go-live.md`. Staging stays on `supabase.co`. **(a) Publish to Production — DONE 12 Aug 2026.** OAuth consent screen publishing status now reads **In production**, so any Google user can sign in (not just added test users). The sign-in screen correctly shows **`aurasonic.es`** as the destination; Google is separately reviewing the app *branding* (name/logo) — cosmetic and non-blocking, sign-in works meanwhile. | ✅ Both done. | ✅ |
| ✅ | 123 | **Show the artist's profile photo on feed reels** | **Shipped 12 Aug 2026.** The For You reel card (`ContentReelCard`) now shows the artist's real **profile picture** in the avatar circle (`<Image fill>`), falling back to the note icon only when there's no photo (or for a MadGigz announcement). `fetchContentPosts` + `fetchShowContent` embed it via the `content_posts.artist_id → profiles` FK (`select("*, profiles(artist_photo_url)")`); `ContentPost` carries `artistPhotoUrl`, set by `mapContentPost`. No new grant (`artist_photo_url` is already public-API-readable) and no migration. Verified: the live embed returns real photo URLs against prod data, `next/image` already allow-lists `*.supabase.co`, types/lint/build clean. **Announcements** keep the MadGigz logo (`AnnouncementCard`) by design. | Done. | S |
| 9 | 124 | **In-app poster creation when posting an event** | Today Add Show (`profile/add-show`, and the admin event form) takes an **uploaded** poster — so an artist without design tools is stuck. Give them a way to **generate a poster in-app** from the show details they're already typing: title, artist/line-up, venue, date/time, price, on **brand templates** (the maroon/orange/cream palette, Galdern display type — the same on-brand look `AnnouncementCard` already draws live for text announcements, and `scripts/make-posters.mjs` does offline). Likely shape: a few selectable templates, live preview, rendered client-side to an image (Canvas, or html-to-image on a styled node), then fed into the existing `uploadEventMedia` path as the event's poster — so it's just another image downstream, no schema change. Nice-to-haves: pick an accent/background, drop in the artist photo (#123) or a supplied image as the backdrop, auto-layout the text. **Adjacent to #99** (the in-app video editor) — both are "create the media in-app instead of leaving for another app"; this is the poster/image half and is far simpler (no ffmpeg/WebCodecs, just canvas). Keeps upload-your-own as the alternative. | Nothing technical — canvas/DOM-to-image render + the existing upload path. Needs a couple of template designs. | M |
| ♻️ | 125 | **Rotate the Apple Sign-in client secret (~every 6 months)** | **Recurring ops, not a feature.** The Apple provider's "Secret Key (for OAuth)" in Supabase (Auth → Providers → Apple) is a **JWT that Apple caps at 180 days** — when it lapses, **Sign in with Apple silently stops working** (no error until a user tries). To rotate: re-run Supabase's generator on their [Apple docs page](https://supabase.com/docs/guides/auth/social-login/auth-apple) with the **same inputs** — Team ID, Services ID (`es.aurasonic.madgigz.web`), Key ID, and the **`.p8`** file contents — copy the new JWT, paste into Supabase → Apple → Secret Key, Save. **Keep the `.p8` safe** (Apple only lets you download it once; it's the input every rotation needs — losing it means making a new key). First secret generated 12 Aug 2026, so **due ~Feb 2027**. Best made a real calendar reminder, not just a backlog line. | Nothing — a 5-minute dashboard task, on a clock. | XS |
| ✅ | 126 | **App Store Connect: declare EU trader status (DSA)** | **Done 13 Aug 2026** (Vir completed the Business section). Under the EU **Digital Services Act**, AuraSonic SL is declared as a **trader** in App Store Connect → **Business**, with the trader name/address/contact Apple displays publicly — the requirement that gates EU App Store distribution. | Done. | S |
| ✅ | 128 | **Native iOS: "logged out" on every app close** | **Fixed 12 Aug 2026 — and it was never a session/cookie bug.** Safari Web Inspector on the device proved the `sb-*` auth cookies persist across a full app kill (2027 expiry, present before and after). The real cause: the Capacitor shell's WKWebView cold-launches at `server.url` (the **root**), and the root `LandingPage` rendered the sign-in/role-select **unconditionally**, never redirecting an already-authenticated user — so a valid session showed the sign-in page every launch. The web PWA dodges it because `start_url` is `/feed`. Fix: `src/app/(onboarding)/page.tsx` now `getUser()`s and redirects a signed-in user to `/feed` (or their pending `next`); the `(app)` layout still handles not-really-authed (→ `/`) and unfinished-onboarding (→ `/signup/complete-profile`). **Web change, so it fixes the installed app on next launch — no rebuild.** Two earlier native cookie-bridge attempts were reverted (stock AppDelegate/SceneDelegate) — they chased a non-existent persistence problem. Lesson: diagnose with Web Inspector before writing native cookie code. | Done. | S |
| 4 | 129 | **Offline ticket access** | Requested from the internal test (12 Aug 2026): a fan should be able to open a **purchased ticket (QR + show details)** with no connection — venues have bad signal, and a ticket you can't load is a ticket you can't use. The QR encodes just a ticket UUID string (e.g. `73202d65-…`), so the fix is to **cache each ticket's data locally** (Cache API / IndexedDB / localStorage) and render the QR offline from the cached UUID + show fields. Extends the #110 service worker, which **deliberately never caches authed HTML** — so this needs a *targeted* ticket cache (only the user's own tickets, refreshed when online), not blanket caching of authed pages. Benefits web PWA and the native shell equally. **Extension — add to native wallet (Vir, 13 Aug 2026):** let a fan save the ticket to **Apple Wallet** (`.pkpass`) and **Google Wallet** (Wallet API pass), which gives true offline access + lock-screen surfacing for free and is the pattern fans expect from DICE/Eventbrite. This is a **meaningfully bigger job than the local cache** and should probably be its own item: Apple `.pkpass` requires a **Pass Type ID + signing certificate** (Apple Developer) and server-side pass generation/signing; Google Wallet requires a **Google Wallet API issuer account** + a signed JWT "save" link — so it makes sense to **wait until the Google Play/issuer approval lands** (ties to #110) before building the Google half. Sensible split: ship the **local offline cache first** (no external accounts, works now), then layer **wallet passes** as a follow-up once the store/issuer accounts are approved. Both wallets encode the same ticket UUID the QR already uses, so the scan path at the door is unchanged. **Apple Wallet half BUILT 13 Aug 2026 (code) — needs Vir's cert + a device test.** `.pkpass` generation + signing via `passkit-generator` (`src/lib/apple-wallet.ts`), gated on the signing cert env (`src/lib/apple-wallet-config.ts`) so nothing shows until it's set — same pattern as Cloudflare Stream. Route `GET /api/tickets/[ticketId]/pass` auths the caller, verifies they own the (non-refunded) ticket, and returns the signed pass; the QR barcode + serial are the **ticket UUID**, so it scans identically to the in-app QR. "Add to Apple Wallet" button on the ticket QR sheet (opens the pass via `openExternal` → SFSafariViewController, which presents iOS's Add-to-Wallet sheet a WKWebView can't); shown only when configured (`appleWalletEnabled` threaded saved page → SavedClient → TicketQRModal). Pass images embedded as base64 (`scripts/gen-wallet-assets.mjs`) so a serverless function needs no `public/` on disk. Validated end-to-end with a self-signed cert (`scripts/`-style test: 15 assertions — well-formed signed zip, pass.json fields, barcode == uid, null-when-unconfigured). Setup runbook: `docs/apple-wallet.md`. Strings `ticket.addToWallet` (en+es). **Auth fix 13 Aug 2026:** first device test returned **401 Unauthorised** — the native "Add to Wallet" opens the pass in **SFSafariViewController**, a separate browser whose cookie jar doesn't hold the app's `sb-*` login, so the cookie-gated route rejected it (same isolation family as #132). Fixed with a **signed short-lived token**: a server action (`wallet-actions.ts`, runs in-app where the login exists) verifies ownership and mints `/api/tickets/<id>/pass?t=<hmac>`; the route accepts the token **or** a same-origin session cookie (web), then reads the ticket via the service role. `src/lib/wallet-token.ts` (HMAC over ticketId+expiry with the service-role key; 15-min TTL; 7 unit tests). **Local offline cache BUILT + verified 16 Aug 2026.** While online the Saved page mirrors the fan's own usable tickets (non-refunded, upcoming) into `localStorage` (`src/lib/offline-tickets.ts`) — barcode (`qr_secret`) + show fields only, no price/PII. A standalone **`/tickets`** page (`src/app/tickets/`) sits OUTSIDE the authed `(app)` group so a navigation survives with no network; it reads the cache and renders the QR entirely client-side (same `qrcode` lib + look as the live sheet). Service worker (`public/sw.js`) network-first-caches `/tickets` (safe: it's not per-user HTML) with an `ignoreVary` offline fallback; `offline.html` links to it; the Saved tickets tab has an "Open your tickets offline" link that also warms the SW cache. Cache is cleared on sign-out (`ProfileClient.handleLogOut`). Strings `offlineTickets.*` + `savedPage.availableOffline` (en+es). Verified in-browser: cache load → ticket card → QR modal renders a real data-URL QR from localStorage alone. **Remaining:** Vir sets the 5 Apple-Wallet env vars on Vercel, redeploys, re-tests on device; then the **Google Wallet** (issuer account) half. | Apple: Vir's cert + device test. Google Wallet: issuer approval. | M (cache) ✅ · L (wallet) |
| ✅ | 130 | **Bottom sheets swipe-down to dismiss** | **Shipped 12 Aug 2026, verified on device.** New `useDragToDismiss` hook (`src/components/ui/`) — pointer-based, downward-only, closes past a threshold and snaps back otherwise — wired into **every** bottom sheet with a grab handle: both ticket sheets, profile Settings, delete-account, feedback, add-content (mobile only; desktop stays a centred dialog), manage-show, report, and the mobile announcements sheet. Handlers sit on the handle, not the card, so they never fight a sheet's own scroll. Tap-outside/✕ close kept. Web + native (web change, live without a rebuild). | Done. | S |
| ✅ | 127 | **App Store Connect: answer the new social-media age-rating questions** | **Done 13 Aug 2026** (Vir completed the questionnaire; rating set). MadGigz has **user-generated content + social features**, so the new social-media age-rating questions in **App Information → Age Rating** were answered honestly (UGC + #96 moderation), producing the app's rating — the prerequisite for submitting for review / external TestFlight. | Done. | S |
| ✅ | 131 | **Native iOS: Stripe checkout leaves the app and returns to the browser** | **Shipped + device-verified 13 Aug 2026** (build `1.0 (2)`: bought a test-mode ticket, stayed in-app the whole time, landed on the ticket — no Safari). Not a payments bug (Stripe itself works): Capacitor punts any off-origin navigation to the system browser, so `checkout.stripe.com` opened externally and the return URL (on our own domain) stayed there. Fix: `server.allowNavigation: ["*.stripe.com"]` in `capacitor.config.ts` lets the **main webview** navigate Stripe in-app, so Checkout + Connect payout onboarding run inline and the return URL lands back in the app on its own — no server/checkout-code change, no deep link. Test-mode 3-D Secure stays on `*.stripe.com`; *(live-mode 3DS can bounce to arbitrary bank domains — revisit before the payments go-live, #98/#99.)* | Done. | M |
| ✅ | 131b | **Native iOS: external ticket links open in an in-app browser (with an escape hatch)** | **Shipped + device-verified 13 Aug 2026** ("works as it should"). External-ticketing shows send fans to third-party sites (Entradium, DICE…), which we can't allowlist — so instead of punting to Safari, they open an **in-app browser sheet** (`Browser.open` → SFSafariViewController) that keeps the fan in MadGigz and carries iOS's built-in **"open in Safari"** button. New `openExternal()` in `src/lib/native.ts` (web = new tab, unchanged; native = in-app sheet), wired into `TicketModal` and `PublicEventActions`. Web-bundle change (the `@capacitor/browser` plugin was already in the binary), live on the installed app via prod. | Done. | S |
| ✅ | 132 | **"Check your email" screen needs a way back to sign in (+ verification didn't complete)** | **Shipped + device-verified 13 Aug 2026** — the dead end is gone; the residual password re-entry is tracked as its own item (#134). Reported from the internal test (12 Aug 2026): after signup the account **read as unverified in the app** and the "check your email" screen was a **dead end**. Diagnosed: the confirmation link in the inbox opens in **Safari**, not the app's WKWebView — `/auth/confirm` verifies the account and sets the session cookie **in Safari's cookie jar**, which the app's separate webview never sees, so the fan returns to the app still logged out. The account *is* verified; the app just didn't receive the session. **Fix shipped in code 13 Aug 2026 (web bundle, no rebuild):** the verify-email screen now has an **"Already confirmed? Sign in"** link → `/signin` (carrying `email` + `next`), and `/signin` prefills the field from `?email=`. So the fan confirms in Safari, comes back, taps Sign in, and logs in with the email+password they just set — in-webview, where the session lands. Verified on device 13 Aug 2026: the sign-in path works and the email is prefilled — so the dead end is gone. **Residual friction (accepted for beta):** the confirm link still opens in **Safari** (iOS hands `https://` links to the browser unless the app claims them), so confirmation lands in Safari's cookie jar and the fan must still **re-enter their password** on returning to the app. Two ways to remove it completely, both **deferred** (bigger than a tweak): **(a) Universal Links** — Associated Domains entitlement + host `apple-app-site-association` at `madgigz.aurasonic.es/.well-known/`, so the email link opens the **app**, confirmation runs in-webview, and the fan goes straight to the feed (no Safari, no password); the proper pre-public-launch fix. **(b) Email OTP code** — put `{{ .Token }}` (6-digit code) in the Supabase confirmation template and add a code input to the verify-email screen; `verifyOtp` runs in-app → session lands in-app, no browser at all (medium effort, also benefits web). Relates to #78. | Me (done — sign-in path; a/b deferred), device-verified. | S |
| ✅~ | 133 | **Notifications screen feels a little slow on device** | **Fixed in code 13 Aug 2026 (web bundle, no rebuild) — awaiting device confirmation.** Diagnosed: the `/notifications` route is a per-request server component, and it had **no `loading.tsx`** — so the App Router kept the *previous* screen frozen on the wire until the fetch finished, which read as "the bell is slow." The query itself is fine (one indexed query, `limit 50`). Fix: added `src/app/(app)/notifications/loading.tsx` — a Suspense skeleton that paints **instantly on tap** so the navigation feels immediate; the real list swaps in when the fetch lands. (SideNav already links it via `<Link>`, so it's prefetched too.) *Follow-up option: the same `loading.tsx` trick would help Explore/Tickets if they ever feel slow.* | Me (done), device-confirm on relaunch. | S |
| 3 | 134 | **Native iOS: Universal Links so the email-confirm link opens the app (kills the signup password re-entry)** | Chosen 13 Aug 2026 as the proper fix for #132's residual friction (Vir: "option 1 is better"). Today the email confirmation link opens in **Safari**, so the session lands there and the fan must re-enter their password back in the app. Fix: claim `madgigz.aurasonic.es` links for the app via **Universal Links** — add the **Associated Domains** entitlement (`applinks:madgigz.aurasonic.es`) in Xcode, and host an **`apple-app-site-association`** JSON at `https://madgigz.aurasonic.es/.well-known/apple-app-site-association` (served as `application/json`, no redirect) listing the app's `TEAMID.es.aurasonic.madgigz` and the `/auth/confirm` (+ likely `/auth/callback`, `/e/*`) paths. Then a tapped confirm link opens the **app**, `/auth/confirm` runs in-webview, the session lands in-app, and the fan goes straight to the feed — no Safari, no password. Also improves shared `/e/<id>` links (open in app). **Part of the public-release phase; needs a rebuild + the AASA file deployed on prod.** Alt considered and rejected for now: email OTP code (#132 option b). | Me (entitlement + AASA route + verify), you (Xcode capability + device test). | M |
| ✅~ | 135 | **Feed reels slow to start playing (cold first-play latency)** | **Fixed in code 13 Aug 2026 (web bundle, no rebuild) — awaiting device confirmation.** Root cause in `ContentReelCard.tsx`: the `<video>` had a `poster` but **no `preload`** and nothing prefetched the next reel, so each clip only began loading when it scrolled into view AND `play()` fired — the first reel fetched cold from Supabase Storage before playing. Fix: `FeedClient` now tracks the in-view reel (`activeIndex` from `scrollTop / clientHeight`, rAF-throttled) and passes `preload="auto"` to the **active reel ±1**, `"none"` to the rest — so the current + next clips buffer ahead (instant next swipe) without streaming the whole feed (egress, #101). Poster still paints immediately. Longer-term adaptive transcode still ties to #99/#101. Confirm time-to-first-frame on device. | Me (done), device-confirm. | M |
| 1 | 137 | **Performance & scale — make the app feel fast (and stay fast at 1000s of users)** | **Raised by Vir 13 Aug 2026** after the perf batch: Stripe felt faster, reels still slow, and the bigger worry is overall speed at scale. **The key distinction: two different problems wearing one coat.** (A) **Perceived latency** — how snappy each interaction feels — which is what's felt now. (B) **Throughput at scale** — 1000s of concurrent users. They are *not* the same, and more users does **not** make the app slower per-user until servers saturate; `docs/load-and-capacity.md` already measured ~100 concurrent browsing as comfortable (p95 ~0.7s, Vercel burst ceiling 1000). So today's slowness is **latency, not scale** — but the fixes overlap heavily (**caching + CDN + smaller media** helps both). **Why it feels slow, biggest lever first:** (1) it's a **remote-URL Capacitor shell** — the app is a WKWebView pointed at prod, so *every* navigation is a network round-trip to Vercel (SSR) + Supabase; there's no local app bundle and no instant client-side nav. (2) **Video delivery** — reels are raw files straight from Supabase Storage, no transcode/adaptive-bitrate (HLS), so a big file must fetch cold before first frame (iOS also throttles `preload` on cellular); today's active±1 preload helped the *next* reel, not the *first*. (3) **No client-side data cache** — each screen refetches on every visit (no SWR/React-Query). (4) SSR/serverless cold starts. **The plan (cheap→deep):** **P1 Media pipeline** — compress/transcode video on upload + serve via a CDN (Supabase Smart CDN / bunny / Cloudflare); the single biggest felt win for reels *and* the biggest egress saving (ties #99/#117/#101). **P2 Data/query caching** — cache the read-heavy feed/Explore queries (edge cache / short revalidate / a cache layer) so 1000 users don't each hit Postgres; cuts TTFB and DB load together. **P3 Client-side caching + prefetch** — SWR/React-Query so re-visiting a screen is instant; more `loading.tsx` skeletons (like #133) for perceived speed. **P4 (deep, architectural)** — reconsider the remote-shell for core surfaces: a client-rendered SPA/PWA (or React Native) so navigation is local/instant and only *data* crosses the wire — the real ceiling-lifter, big rebuild, a Phase-2 decision. **P5 Scale-readiness** — audit Supabase connection pooling (Supavisor), re-run the load probe at 500/1000, watch Supabase compute headroom before a marketing push. **Do this thinking with real feedback, not in a vacuum** — instrument first (measure TTFB + time-to-first-frame) so effort lands where the milliseconds actually are. | Vir + me — sequence P1→P3 first (latency), P5 before a big launch. | XL (epic) |
| ✅ | 138 | **Cloudflare Stream for reels (video transcode + adaptive HLS + CDN)** | **WORKING 13 Aug 2026 — verified end to end on BOTH posting paths** (feed "+" and the manage-show sheet; DB confirmed `stream_uid` set + videos in Cloudflare Stream). Video now transcodes to adaptive HLS on Cloudflare's CDN with auto thumbnails; images stay on Supabase. Single shared upload path `src/lib/content-upload.ts`. See [[project_cloudflare_stream]]. **Phase-4 follow-ups (own item #139):** delete the Stream video on post-delete (orphan cleanup); raise the video upload cap now Stream shrinks files (Vir's "compress big files" ask); optional backfill of legacy Supabase reels. Also worth a look: reel HLS playback feel on device (the whole point — faster reels). After a long dead end (config, token, migration, server all verified perfect via a temp `/api/stream-check` route, yet no video reached Cloudflare from the app *or* a private browser), the real cause: **two content-posting paths.** Stream was wired into `AddContentModal` (feed "+") but NOT `ManageShowModal` (post-from-show sheet), which the artist actually uses — so those reels uploaded straight to Supabase (`media_url` set, `stream_uid` null) with no error. Fix: extracted **`src/lib/content-upload.ts` (`uploadContentMedia`)** as the single upload path — video → Stream when configured (throws on genuine failure so it can't silently fall back and hide this again), images → Supabase — and wired **both** modals to it. Removed all temp diagnostics (the `/api/stream-check` route, verbose errors, middleware exclusion). **Lesson: grep for *every* insert/upload path before assuming one call site is the whole feature.** Next: Vir posts a video from the show sheet → confirm it lands in Cloudflare Stream. **Prior status:** core built + build-verified 13 Aug 2026 (dormant until turned on). Shipped: `addendum_035_stream_uid.sql` (+ staging_full_setup), `src/lib/cloudflare-stream.ts` (HLS + thumbnail URL builders, public customer code baked in), `src/app/(app)/feed/stream-actions.ts` (`createStreamDirectUpload` server action — reads the secret token, returns `null` when unset so upload falls back to Supabase), `AddContentModal` video path (→ Stream direct upload, else Supabase), `ContentReelCard` (HLS playback: native on iOS, `hls.js` on desktop; Stream thumbnail as poster — also fixes the black-frame-until-decode bug), types + `stream_uid` wiring, i18n `addContent.errorUpload`. **Everything is gated: the Stream path can't run until BOTH (a) the migration adds the column AND (b) `CLOUDFLARE_STREAM_TOKEN` is set — so shipping it changed nothing yet.** **Turn-on order (Vir):** ① run `addendum_035` on **staging** Supabase, then **prod** Supabase (additive, safe); ② add `CLOUDFLARE_STREAM_TOKEN` to Vercel (Production + Preview) + `.env.local`, then redeploy; ③ post a test video reel → confirm it plays via HLS with a thumbnail. **Remaining (phase 4, follow-up):** delete the Stream video on post-delete (extend the delete path, needs the token server-side); raise the video upload cap (`MAX_CONTENT_FILE_BYTES` is shared with images — add a separate larger video cap now that Stream shrinks it); optional backfill of existing Supabase reels. **Security:** rotate the API token Vir pasted in chat once confirmed working. **Original plan below.** P1 of #137, approach chosen 13 Aug 2026 (Vir: happy to pay for Cloudflare if it improves the experience). Replaces raw Supabase-Storage video with **Cloudflare Stream**: upload → transcode → adaptive-bitrate HLS served from Cloudflare's CDN, with auto-generated thumbnails. Fixes reel first-play latency + egress + big-file uploads in one, and **gives videos a real poster** (today `mapContentPost` leaves `image=""` for video posts, so reels show a black frame until decode — `ContentReelCard poster={post.image}` is empty). **Images stay on Supabase** (Stream is video-only). Phases: **(0, Vir)** create Cloudflare account, subscribe to Stream, supply Account ID + a Stream:Edit API token + the `customer-<code>` playback subdomain → env: `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_STREAM_TOKEN` (secret), `NEXT_PUBLIC_CLOUDFLARE_STREAM_CUSTOMER_CODE` (public). **(1)** migration: `alter table content_posts add column stream_uid text` (nullable; existing videos keep `media_url` on Supabase and still play — graceful fallback, catch `42703`). **(2)** upload: server action → Stream **direct-upload URL** (`POST /accounts/<id>/stream/direct_upload`), client uploads the file straight to Stream, store the returned UID; `AddContentModal` video path switches from `uploadEventMedia`. **(3)** playback: `ContentReelCard` plays `https://customer-<code>.cloudflarestream.com/<uid>/manifest/video.m3u8` (iOS/WKWebView plays HLS natively; add **hls.js** for desktop Chrome/Firefox) with the Stream thumbnail as poster; handle the brief post-upload "processing" window (show poster/thumbnail until ready). **(4)** cleanup: delete the Stream video on post delete (extend `removeEventMedia`), and raise `MAX_CONTENT_FILE_BYTES` for video since Stream shrinks it. Existing Supabase-hosted reels are left as-is (optional backfill later). | Done. | L |
| ✅ | 139 | **Cloudflare Stream — phase-4 polish (follow-up to #138)** | **Shipped 13 Aug 2026.** **(a)** Orphan cleanup: `deleteStreamVideo(uid)` server-only helper (`src/lib/cloudflare-stream-server.ts`, best-effort, never blocks the surrounding delete), wired into **all three** delete paths — artist post-delete (`ManageShowModal`, via the ownership-checked `deleteReelStreamVideo` action so a uid can't be used to delete someone else's video), account deletion (`account-deletion.ts`), and admin event hard-delete (`admin/actions.ts`; the soft-cancel path keeps posts, so no cleanup there). **(b)** Video upload cap raised to **200MB** (`MAX_VIDEO_FILE_BYTES`; Stream's basic direct-upload ceiling) via shared `maxBytesForMediaType()` used by both posting modals; images stay 50MB; `errorTooLarge` now interpolates `{mb}`. Delivers Vir's "upload files too big to compress" ask — Stream shrinks them. **(c) deferred:** backfill of legacy Supabase reels (optional, low value). **(d):** confirm HLS reels feel faster on device (ties #135/#137) — Vir to eyeball. | Done. | M |
| ✅ | 140 | **Cloudflare Stream: TUS resumable upload (lift the 200MB video cap)** | **Shipped + verified on device 13 Aug 2026** (Vir: percentage ticked up, the >200MB video reached Cloudflare, transcoded/compressed, and plays — test passed). The shared `uploadContentMedia` now **size-branches**: ≤200MB keeps the proven basic direct-upload POST (untouched, so the common reel is unchanged), and **>200MB uses Cloudflare Stream's TUS resumable protocol** via `tus-js-client` (dynamically imported so it only loads for big files). New server action `createStreamTusUpload(bytes)` mints the upload with `POST /accounts/<id>/stream?direct_user=true` (Tus-Resumable + Upload-Length + base64 Upload-Metadata `maxDurationSeconds 600`), reads the `Location`/`stream-media-id` response headers, and hands the client a one-time URL to PATCH chunks to — **the secret token never reaches the browser**. 50 MiB chunks (Cloudflare's 256 KiB-multiple / 5 MiB-min rule). Both modals show **live upload %** (`addContent.uploadingPercent`, en+es) driven by an `onProgress` callback; resumable means a dropped mobile upload continues instead of restarting. `MAX_VIDEO_FILE_BYTES` raised **200MB → 5GB** (`STREAM_MULTIPART_MAX_BYTES` is the new split point). Delivers Vir's "let people upload files too big to compress" — Stream still transcodes it down for delivery. Build/lint/knip clean. **Verify:** post a **>200MB** clip → confirm it lands in Cloudflare Stream, the % ticks up, and it plays via HLS (needs `CLOUDFLARE_STREAM_TOKEN` set, which it is). Distinct from in-app *pre-upload* compression (WebCodecs), which belongs with #99. | Me (done) — Vir to test a big file. | M |
| ✅ | 141 | **Past vs upcoming shows — separate them everywhere (past-gig infrastructure)** | **Shipped 13 Aug 2026.** Split on `event_date >= today` (the convention `admin-queries` already uses), `TODAY` read once at module load per client file. **(a) Public artist profile** (`ArtistShowsGrid` + `[artistId]/page.tsx`): "Upcoming shows" now future-only (soonest first); a separate **"Past shows"** section below (most-recent first) — fixes the screenshot bug (a past gig listed under Upcoming). One shared ticket modal over both. **(b) Own artist profile** (`ProfileClient`): "Your Shows" heading → **Upcoming shows** + a **Past shows** section; hidden shows still folded away. **(c) Tickets tab** (`SavedClient`): "My Tickets" is upcoming-only — a **past, unattended** ticket just drops off (per Vir: it's neither where you're going nor where you've been); **"Where you've been"** (check-in-based) unchanged. **(d)** "Add to Wallet" button hidden on past shows. Also renamed the button **"Add to Apple Wallet" → "Add to Wallet"** (en+es) — the `.pkpass` adds to **Google Wallet on Android** too (Vir confirmed on the A07), so one generic button covers both platforms (and simplifies the Google Wallet half of #129). New i18n `profile.upcomingShows`/`pastShows`. Build/lint/knip clean. | Done. | S–M | (with a screenshot: an artist's **"Upcoming Shows"** listed *Hard Fuse Rock Battle · Mon 10 Aug*, which already happened). Today several surfaces mix past and future shows under one "upcoming" heading; split them by `event_date < today`. **(a) Artist profile (own + public `/profile/[artistId]`):** "Upcoming Shows" must show only future dates; add a separate **"Past shows"** section below for dates that have passed — the screenshot bug is a past show appearing under Upcoming. **(b) Tickets tab (`SavedClient`):** add a **"Past" label/section** for tickets whose *show date* has passed, kept **below all upcoming tickets**. Note this is date-based (`event_date < today`), distinct from the existing "Where you've been" which is check-in-based (`checked_in_at`) — decide whether they merge or coexist. **(c) Fan profile:** the "fun poster wall" (#116, already shipped there) is the right home for the memories/past-shows visual — keep it on the profile, **not** the tickets side; the tickets tab just needs the plain past label. **(d) Tie-in:** hide the "Add to Apple Wallet" button for past shows (a wallet pass for a gig that already happened is pointless — flagged during the #129 wallet work). Mostly a set of `event_date`-vs-today filters + section headings; the data (`event_date`) is already there. | Nothing technical — filters + headings. | S–M |
| ✅~ | 136 | **Pull-to-refresh on the feed (pull down on the top reel to refresh)** | **Built in code 13 Aug 2026 (web bundle, no rebuild) — awaiting device confirmation** (the gesture only exercises on a real touchscreen). In `FeedClient`: native, **non-passive** touch listeners on the reel scroll container (React attaches `touchmove` as passive, so `preventDefault` — needed to suppress iOS rubber-band / Android's own PTR — wouldn't take otherwise). Fires only when `scrollTop === 0` and the pull is downward; applies resistance + a cap; past a 72px threshold it refetches `fetchContentPosts`. A floating spinner follows the pull (rotates with distance, spins while refreshing) rather than transforming the snap track (which would fight scroll-snap). Added `overscroll-y-contain` so a browser's own PTR doesn't double-fire. Desktop is a no-op (no touch). | Me (done), device-confirm the gesture. | M |

## Why this order

**82a shipped on 10 Aug 2026**, and the split paid off exactly as intended:
the post-callback screen, the rewritten signup trigger and the
`complete_onboarding()` door are all provider-agnostic, so 82b really is a
provider toggle and a button whenever Apple gets round to Vir's verification.
It stays at the top because it is nearly free the moment it unblocks — not
because it is the most valuable thing here.

**#59 shipped (Aug 2026).** Every non-admin surface reads from a typed
en/es message catalog (`src/lib/i18n/`), locale is a cookie with
Accept-Language auto-detect and a manual switch in Settings, and the admin
panel stays English by design. Dates render `en-GB` and prices EUR
throughout — deliberately left, not forgotten.

One thing still needs a person, not code: the Spanish beyond the first
tranche is **my draft** and wants the same fluent-review pass the reviewer
gave the opening screens. The review sheet at
`docs/madgigz-translation-review.pdf` now covers all 487 strings (23 pages,
English | Español | correction), and it is generated from the live catalog
(`node scripts/export-i18n-json.mjs && python3
scripts/make-translation-review-pdf.py`), so it can't drift. Relay any
corrections and I'll apply them and regenerate.

**#95 is a subdomain, not a migration.** `madgigz.aurasonic.es` points at the
existing Vercel deployment: one DNS record at whoever hosts `aurasonic.es`, one
domain added in the Vercel project, and Vercel issues the certificate itself.
The apex is untouched, so the main AuraSonic site can live wherever it likes,
and Supabase does not move — it was always separate hosting.

The DNS part takes minutes. The part worth care is the settings that have to
move with it, because **each one fails silently rather than loudly**:

- `NEXT_PUBLIC_APP_URL` — every shared event link and link preview is built from
  it (`src/lib/site.ts`). Leave it and shares keep pointing at the old
  `.vercel.app` host, which still works, so nobody notices for weeks.
- **Supabase → URL Configuration** — the new origin needs adding to Redirect
  URLs and Site URL, or Google sign-in returns people to the site root with no
  session. Google Cloud itself needs no change: its redirect URI is Supabase's
  `/auth/v1/callback`, which doesn't move.
- **Stripe** — live keys, and a *separate* webhook endpoint registered against
  the new URL with its own signing secret. Test-mode webhooks do not carry over,
  and a missing endpoint means money is taken and no ticket is issued.
- Email needs nothing: Resend already sends as `@aurasonic.es`.
- **Unset `ALLOW_ADMIN_IMPERSONATION`** before launch — it's the admin "act as
  any user" testing tool (`docs/impersonation.md`). Off by default, but if it was
  turned on for testing, launch is when it must go back off.

Worth doing before this: the subdomain is also what a native shell would point
at, so picking it now means the URL doesn't change again when MadGigz becomes a
real app. Same reasoning as staying on web for launch — see the note on
Capacitor from 10 Aug 2026.

**#89 and #91 are one feature, and the risk is in the rules.** The form is an
afternoon; deciding what happens to the old handle is the part worth thinking
about. Releasing it immediately means someone can claim an artist's former
username the moment they change it, which is how impersonation starts.

**Vir decided this on 9 Aug 2026: a released username is held for 10 days
before anyone else can claim it.** Shorter than the 30 days most platforms use,
which is a deliberate trade — it frees good handles faster, and MadGigz is small
enough that a targeted impersonation attempt would be noticed. Implement it as a
`username_history` row (profile id, the old username, released_at) checked by
`username_available()`, rather than a flag on the profile: the profile can only
remember one previous name, and someone who changes twice in a fortnight would
otherwise release the first one instantly. Public
profiles are routed by id (`/profile/[artistId]`), not by username, so a rename
breaks no existing links — that part is already safe.

Doing #91 in the same pass matters because it changes what a username *is*: once
it's a login credential, a rename is a credential change, and the two features
have to agree about cooldowns and about what a freed username can be reused for.
Building them months apart means revisiting the same decision twice. The lookup
itself must live server-side and never return the email to the browser — it
signs the person in, it doesn't tell the caller what address it used.

**#88 turns on one decision: ownership.** `events.artist_id` assumes an artist
owns a show, and every RLS policy keyed on `events.artist_id = auth.uid()`
widens with it — events, event_artists, content_posts, and the whole checkout
path. Generalising it (an `owner_id` + `owner_type`, or a separate table) shapes
everything else, so settle it before any UI gets designed. The other two
questions are commercial rather than technical, and yours: who gets paid when a
promoter books an artist and whether the 5% splits three ways, and whether a
venue sees sales for shows at their venue they didn't book.

**#92's hard problem is payouts, not profiles.** Linking accounts together is
a `band_members` table and an afternoon. But a Stripe destination charge pays
exactly **one** connected account — so the moment a band sells a ticket, the
money has to land somewhere specific. That forces a product decision before any
code: either the band nominates one member's payout account and splits offline
(simple, and how most small acts already operate), or MadGigz splits at
checkout, which means Stripe Connect transfers per member, per sale, and a
share-percentage UI that every member has to agree to. The second is a
meaningfully bigger product with tax implications for each member — worth
choosing deliberately rather than discovering halfway in.

Two smaller questions ride along: whether a band show appears on each member's
own profile as well as the band's, and whether the artist verification flow runs
on the band or on each member (a band anyone can join is an impersonation route
into an established name). Neither is hard, but both shape the schema.

**#90 is the app's whole premise, so build the frame now even though Madrid is
the only city.** Explained by Vir on 11 Aug 2026: MadGigz is local — a fan opens
it and sees *their* city's shows, told plainly ("You're in Madrid"). Launch is
Madrid-only and new cities are turned on one at a time. The data is ready
(`events.city`, `venues.city`, both default `'Madrid'`), so this is three
decisions, not a schema problem:

1. **What "cities exist" means.** A small `cities` table (or an allow-list) with
   a launched flag, so switching on Barcelona later is a data change, not a
   deploy. Everything already says Madrid, so seeding it is trivial.
2. **How a fan's current city is chosen.** Options, cheapest first: a manual
   picker that defaults to Madrid and is remembered (a cookie, like locale);
   browser geolocation → nearest launched city as a *suggestion* only (needs a
   permission prompt and a reverse-geocode, and must fall back gracefully);
   or IP-based (rough, no prompt). MVP is the picker — with one city it's just a
   label, but it puts the plumbing in. Geolocation is a later nicety, and it's
   personal data, so it lives under the same GDPR rules as #58.
3. **Where the filter goes.** Feed, Explore and This-Week query by the current
   city instead of all events; the city is named in the header. One filter added
   in one place, because the column already exists.

**The two "later" halves the frame has to leave room for:** a fan *switching*
city while travelling (the picker becomes a real control, and "your city" vs
"the city you're browsing" become two different things — saved/tickets stay
yours, the feed follows the browsed city), and a touring artist *posting into
another city* (the Add-Show venue picker can't stay locked to one city, and a
venue may need creating in a city the artist doesn't live in). Neither is MVP,
but choosing the "current city" representation now (a value the whole app reads,
not a hardcoded `'Madrid'`) is what keeps both from being a rewrite. Size is L
because of these, not the launch cut, which is closer to M.

**#58 collects personal data.** Geolocation and login history are personal data
under GDPR, so they fall under the retention and erasure rules `addendum_019`
set up — the purge in `src/lib/account-deletion.ts` would have to scrub whatever
this adds. Decide what it's *for* before building it: "useful someday" is a poor
reason to start retaining people's locations.

**#97 and #98 are both "when there is money to justify it".**

Tax (#97) is the one that arrives on its own schedule: the moment real euros
move, somebody has to account for IVA on the MadGigz fee and artists start
needing invoices for their accountant. Stage 6 explicitly parked VAT as "an
accountant's question, not a guess in code" and that is still the right call.

**Design worked out with Vir on 11 Aug 2026 (planning ahead, gestor pending).**
The frame: **Odoo is the invoice engine, MadGigz/website is the data-capture
layer, the gestor signs off the tax.** MadGigz never generates a legal factura —
it feeds Odoo accurate counterparties, and Odoo issues the documents. This is
the only way to build any of it without guessing Spanish tax rules in code.

_Two distinct invoice flows, and they are not the same job:_

- **A — Artist-owned shows sold through MadGigz.** Money already flows
  fan→artist via the Stripe destination charge, minus MadGigz's 5% + 21% IVA.
  What's owed is a **service-fee factura, MadGigz → the artist** (or the org that
  represents them). Needs the artist's fiscal identity, or an organisation to
  bill instead.
- **B — House shows (MadGigz owns the event).** MadGigz is the seller, so it must
  **issue sales invoices for the tickets** (B2C — likely _factura simplificada_
  per ticket, full factura on request; IVA rate on live-music tickets is a
  gestor question). _Then_ MadGigz **pays the performing artist**, and this is
  where "payouts get more complicated": the artist may not be autónomo and gets
  paid via a **cooperativa de facturación** (ARTiCAT, Xº Décimo Arte, SMart,
  etc.) that employs them for the event days and **invoices MadGigz under its own
  CIF**. So the payee is often the cooperativa, not the artist — and Stripe
  Connect doesn't apply here because MadGigz collected the ticket money itself.

_The entity model MadGigz needs (the part that's ours to build, no tax guesses):_
a private **fiscal identity** per artist (entity type individual/company/coop,
legal name, NIF/NIE/CIF, fiscal address, IVA regime) — stored owner-only +
service-role, **never on the public `profiles` table** (it's exactly the
personal-data class CLAUDE.md warns about); an **organisations** store (promoters,
agencies, cooperativas — same fiscal fields, admin-managed); and a link from
artist → the entity to bill/pay (possibly per-event, since a touring artist may
use different orgs show to show). Each maps to an Odoo `res.partner`.

_The open architecture fork (Vir, 11 Aug):_ **where does fiscal capture live?**
(1) In the MadGigz app, then sync Supabase→Odoo via the RPC tool; or (2) **on the
Odoo-hosted website directly** (`aurasonic.es` is already Odoo-linked), so the
data lands natively as `res.partner` with no sync bridge and Odoo stays the
single source of truth. Option 2 is cleaner if the artist tolerates leaving the
app for a form — the app then only needs a "fiscal registration complete" flag
read back from Odoo to gate house-selling/invoicing. Leaning (2); decide when
building.

_Questions the gestor has to answer before any of this is built (these gate the
schema and the flows, and are genuinely not a coding call):_
1. Flow A: is a "service fee" factura MadGigz→artist for the 5% + 21% IVA the
   correct instrument? Reverse-charge if the org is in another EU state?
2. Flow B tickets: _factura simplificada_ per ticket vs a periodic summary; which
   **IVA rate** applies to live-music tickets (21% / reduced 10% / cultural
   treatment); and AuraSonic SL as fiscal seller of record.
3. Flow B payout: accepting a supplier invoice from the artist/cooperativa, and
   what **IRPF retención** applies when MadGigz pays a Spanish artist directly.
4. **Verifactu / SII** e-invoicing obligations — these decide whether Odoo must
   be the legal system of record and constrain how invoices may be issued.
5. Exactly which **fields a valid Spanish invoice requires** for each flow, so
   MadGigz captures precisely those and no more.

In-house payments (#98) is the furthest thing on this list and should probably
stay there. Taking payments directly means becoming a payment facilitator:
PSD2/SCA compliance, PCI scope, holding other people's money, and in Spain a
licence to do it. Stripe's cut buys all of that plus the fraud handling and the
Connect payouts. It only becomes arguable at volumes where a percentage point
is real money, and MadGigz is a long way from there.

**#103 shipped (10 Aug 2026): `docs/key-rotation.md`.** Every secret the app
holds, its blast radius if leaked, and a five-step per-secret runbook, all
grounded in the actual env the code reads. The non-obvious part is *order*:
create at the provider → update Vercel env → redeploy → verify `/api/health` →
only then revoke the old value (the reverse takes the app down between steps —
the same two-phase discipline the migrations use). It flags the two traps: the
Supabase JWT-secret rotation is the one heavy case that signs everyone out, and
the Google OAuth client secret lives in the Supabase dashboard, not Vercel env.
Going live (#95) already forces the Stripe half, so the doc's Stripe rows and the
go-live checklist are the same work — do them together.

**#104 pays for itself the moment a convention is applied wrong once.** The rules
that already bit us live in CLAUDE.md prose — column GRANTs, the two-phase
revoke, "read the stored value back" — and a skill turns each into an invokable
checklist instead of something to remember. The i18n add-a-string flow is the
clearest candidate: it's now four coordinated steps (en.ts, es.ts, the JSON
export, the PDF) and easy to half-do.

_First skill shipped 10 Aug 2026: `.claude/skills/simulate-users/` — a
self-contained load / end-to-end harness (`scripts/simulate-users.mjs`) that
onboards N synthetic users and drives real fan sessions under concurrency, then
tears the cohort down. Sends no email, touches no money, and is dry-run by
default. The convention-checklist skills (migration two-phase, i18n add-a-string,
adversarial-probe, "read the Next docs first") are still to come._

**#105 ("web version") needs one sentence from Vir before it's scoped.** The app
is *already* web — so this means one of two different jobs: a wide-screen/desktop
layout for the existing app (multi-column, not a phone in the middle of a
monitor), or a separate marketing site on the apex. They share almost no work.
Parked like #90 until clarified.

## Load & servers — the short version

Assessed 10 Aug 2026 (`docs/load-and-capacity.md`, `scripts/load-probe.mjs`):
**100 people browsing at once is comfortable** — a live read probe held to 60
concurrent page-loads (240 parallel queries) at p95 ~0.7s with zero errors on the
free tier, and Vercel's burst ceiling is 1,000 concurrent, so 100 is ~10%. **100
people onboarding at once is gated by email, not servers**: Supabase's custom-SMTP
signup limit defaults to 30/hour and Resend's free tier is 100 emails/day — both
config/plan settings to raise before any launch push, not code to change.

## To test when you're back

- **Smoke-test fan → artist upgrade** (shipped 10 Aug 2026). As a fan (impersonate
  one via /admin/users), Profile → Settings → "Switch to artist account" → confirm
  → should land on the claim form; submit → appears in the admin artist queue as
  pending → approve it. Built and build-verified, not yet clicked through on a
  live login.

## Nothing else is currently waiting on you

`CRON_SECRET` set, every migration through `addendum_027` run, Stripe keys
rotated, every active venue has an address, both email templates updated in the
dashboard. The house-show path — list, buy, refund, tag, post content, edit —
has been exercised end to end; notifications fire on ticket purchases and
follows; Google sign-up and sign-in both work; and the feedback path has been
walked from the Settings sheet through to resolving it in the admin panel.

Three adversarial probes live in `scripts/` and are worth re-running after any
migration that touches policies or grants — `security-probe.mjs` (what a fan
can do), `probe-artist-side.mjs` (what an artist can do), `probe-feedback.mjs`.
They create and delete their own throwaway accounts. Everything currently
passes; the pattern to keep is that each check reads the stored value back,
because an UPDATE matching zero rows returns no error and "did it error?"
reports a locked door as a hole.
